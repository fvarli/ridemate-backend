*.log
.DS_Store
.env
.env.backup
.env.production
.phpactor.json
.phpunit.result.cache
/.codex
/.cursor/
/.idea
/.nova
/.phpunit.cache
/.vscode
/.zed
/auth.json
/node_modules
/public/build
/public/fonts-manifest.dev.json
/public/hot
/public/storage
/storage/*.key
/storage/pail
/vendor
_ide_helper.php
Homestead.json
Homestead.yaml
Thumbs.db

# Every environment file except the placeholder example.
.env.*
!.env.example

# Private key material — never committed, ignored before any exists.
# Deliberately NOT ignored: *.pem, *.crt, *.cer, which are usually public
# certificates; hiding those would conceal real files rather than secrets.
*.jks
*.keystore
*.p12
*.p8
